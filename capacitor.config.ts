import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'linkshortener',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
